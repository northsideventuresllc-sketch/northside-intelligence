# ReplyFlow — apply on `replyflow` repo

Merge into `.env.local.example` and `README.md` (or cherry-pick branch `cursor/ni-ecosystem-connection-8a4a` if push access is available).

See commit message: *Document NI-Brain and GrantBot sibling links in ReplyFlow*.
