# GrantBot repo scaffold

Push these files to `northsideventuresllc-sketch/grantbot` on branch `cursor/ni-ecosystem-connection-8a4a` (Cloud Agent lacked write access to that repo).

## Files

- `README.md` — NI portal, subdomain, `grantbot_profiles`, ReplyFlow sibling link
- `.env.local.example` — NI-Brain URL `https://kxijunwgbrlfzvgkhklo.supabase.co`
- `.gitignore` — standard Next.js ignores

Supabase table `grantbot_profiles` is already live on NI-Brain (migration `create_grantbot_profiles`).
