# Session start — Sector 3 tool

Read `.cursorrules` and `TEMPLATE.md` first.

1. Confirm which tool repo and branch you are on.
2. List env vars from `.env.local.example` — note any missing locally.
3. Identify Supabase tables with this tool's prefix.
4. Summarize Stripe webhook events and current `TIER` deployment mode.
5. Ask only if blocked; otherwise proceed with the user's task.

Rules: snippets only, no secrets in code, no console logging, error-handle Supabase and Stripe.
